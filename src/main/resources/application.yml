spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?sueSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: 12345678

  jpa:
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace



server:
  port: 8000